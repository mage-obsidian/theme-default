<div class="relative">
    <button type="button" class="inline-flex h-10 w-10 items-center justify-center transition-colors hover:text-ink"
            aria-label="{{ labels.search }}" aria-expanded="false">{{ hero_icon('magnifying-glass', 'outline', '24', 'h-5 w-5') }}</button>
    {% apply island_if(false) %}{% endapply %}
</div>
