{# Credit memo email items. Mirrors Magento_Sales::email/creditmemo/items.phtml. #}
{% set creditmemo = block.getCreditmemo() %}
{% set order = block.getOrder() %}
{% if creditmemo and order %}
    <table class="email-items">
        <thead>
            <tr>
                <th class="item-info">{{ __('Items') }}</th>
                <th class="item-qty">{{ __('Qty') }}</th>
                <th class="item-subtotal">{{ __('Subtotal') }}</th>
            </tr>
        </thead>
        {% for item in creditmemo.getAllItems() %}
            {% if not item.getOrderItem().getParentItem() %}
                <tbody>{{ block.getItemHtml(item)|raw }}</tbody>
            {% endif %}
        {% endfor %}
        <tfoot class="order-totals">{{ child_html('creditmemo_totals')|raw }}</tfoot>
    </table>
{% endif %}
