{# Checkout <h1>. `block` is MageObsidian\Checkout\Block\CheckoutTitle; the
   document <title> is set in checkout_index_index.xml head. #}
<h1 class="font-display text-3xl tracking-display text-ink md:text-4xl">{{ block.getHeading() }}</h1>
