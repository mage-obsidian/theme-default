{# Cart <h1>. `block` is MageObsidian\Checkout\Block\CartTitle, which also sets
   the document <title> in _prepareLayout (the native wiring is suppressed). #}
<h1 class="font-display text-3xl tracking-display text-ink md:text-4xl">{{ block.getHeading() }}</h1>
