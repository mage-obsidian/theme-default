{#
  Placeholder the islands runtime fills with session/AJAX messages after a
  customer-section update, so the full-page-cached HTML stays user-neutral.
  Server-rendered session messages come from the sibling `messages` block.
#}
<div data-placeholder="messages" role="status" aria-live="polite" aria-atomic="true"></div>
