<div class="sticky top-0 z-30 border-b border-ash-200 bg-alabaster/85 backdrop-blur-md print:hidden">
    <div class="shell flex h-[68px] items-center justify-between gap-3">
        <a href="{{ block.getUrl('') }}"
           class="font-display text-base font-semibold tracking-mono text-ink min-[360px]:text-lg min-[360px]:tracking-mono sm:text-2xl sm:tracking-label"
           aria-label="{{ __('OBSIDIAN — home')|escape_html_attr }}">OBSIDIAN</a>

        <div class="flex items-center gap-4 font-mono text-micro uppercase leading-none tracking-eyebrow text-ink-soft sm:gap-6 sm:text-eyebrow">
            <span class="hidden items-center gap-2 sm:inline-flex">
                <span aria-hidden="true" class="inline-block h-1.5 w-1.5 rounded-full bg-accent"></span>
                {{ __('Secure checkout') }}
            </span>
            <a href="{{ block.getUrl('checkout/cart') }}" class="transition-colors hover:text-ink">
                {{ __('Back to bag') }}
            </a>
        </div>
    </div>
</div>
