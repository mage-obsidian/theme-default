{#
  Loads the delegated add-to-cart enhancer on catalog pages (where the cards ship
  add-to-cart forms). The enhancer is progressive: without JS the forms POST
  normally.
  Toast strings are translated client-side via the framework i18n runtime.
#}
{{ script('MageObsidian_Storefront::js/cart-actions') }}
