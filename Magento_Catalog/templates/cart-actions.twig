{#
  Loads the delegated add-to-cart enhancer on catalog pages (where the cards ship
  add-to-cart forms). Rendered by a MageObsidian Template block so vite_url is
  available. The enhancer is progressive: without JS the forms POST normally.
  The i18n strings for the toast are handed to JS here (translated server-side).
#}
<script>window.__OBSIDIAN_CART_I18N__ = { added: {{ __('Added to cart')|json_encode|raw }}, failed: {{ __('Could not add to cart')|json_encode|raw }} };</script>
<script type="module" src="{{ vite_url('MageObsidian_Storefront::js/cart-actions') }}"></script>
