{#
  OBSIDIAN home — designed and server-rendered (the CMS "home" page is left empty
  on purpose; the storefront owns the home composition). The demo doubles as the
  MageObsidian showcase: the home presents the framework — hero, features, stack,
  themes and community — composed from reusable showcase sections. `block` is a
  MageObsidian Template so render_vue / image / hero_icon are available and the
  includes share its context.
#}
<div class="cms-home">
    {% include 'Magento_Cms::home/hero.twig' %}
    {% include 'Magento_Cms::showcase/features.twig' %}
    {% include 'Magento_Cms::showcase/tech-stack.twig' %}
    {% include 'Magento_Cms::showcase/themes.twig' %}
    {% include 'Magento_Cms::showcase/community.twig' %}
</div>
