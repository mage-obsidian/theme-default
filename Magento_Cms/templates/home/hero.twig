{#
  Home hero. CSS/type-driven (no binary asset) on the obsidian art direction:
  vitreous dark field, Bodoni display, the signature `sheen` overlay. Frames the
  framework pitch with two entry points — the docs and the source.
#}
<section class="showcase-bleed sheen relative isolate overflow-hidden bg-obsidian-950 text-on-obsidian">
    <div class="relative z-10 mx-auto max-w-shell px-6 py-28 md:px-8 md:py-40">
        <p class="font-mono uppercase tracking-spaced text-on-obsidian-soft" style="font-size:var(--text-eyebrow)">
            {{ __('Vite · TailwindCSS · Vue · Twig') }}
        </p>
        <h1 class="mt-6 max-w-[14ch] font-display font-semibold leading-[0.95]" style="font-size:var(--text-hero)">
            {{ __('A modern frontend for Magento') }}
        </h1>
        <p class="mt-8 max-w-2xl font-body text-on-obsidian-soft" style="font-size:var(--text-lead)">
            {{ __('MageObsidian replaces Magento’s legacy frontend with Vite, TailwindCSS, Vue islands and Twig — built on the native Layouts, Blocks and Templates you already know.') }}
        </p>
        <div class="mt-10 flex flex-wrap items-center gap-4">
            <a href="https://mage-obsidian.jeanmarcos.dev/" target="_blank" rel="noopener"
               class="btn btn--lg btn--invert gap-3 px-6">
                {{ __('Read the docs') }} <span aria-hidden="true">→</span>
            </a>
            <a href="https://github.com/mage-obsidian" target="_blank" rel="noopener"
               class="btn btn--lg btn--invert-outline gap-3 px-6">
                {{ __('View on GitHub') }} <span aria-hidden="true">→</span>
            </a>
        </div>
    </div>
</section>
